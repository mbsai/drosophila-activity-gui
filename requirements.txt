# Tested with the versions in the right-hand comments. Upper bounds guard
# against a future breaking major release.
streamlit>=1.30,<2      # tested 1.52
pandas>=2.0,<3          # tested 2.3
numpy>=1.24             # tested 2.4
matplotlib>=3.7,<4      # tested 3.10
openpyxl>=3.1,<4        # tested 3.1.5
